{
  "name": "gensql-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm-run-all -p dev:api dev:ui",
    "dev:api": "cd api && uv run uvicorn main:app --reload --port 8000",
    "dev:ui": "cd ui && npm run dev -- --open"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
